'use client';

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#C9AB88',
    },
    secondary: {
      main: '#B1C7C8',
    },
    text: {
      primary: '#342120',
    },
    info: {
      main: '#8E5B40',
    },
    background: {
      default: '#DED1BB',
    },
  },
});

export default theme;
